name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
           login-server: vidyoportalregistry.azurecr.io
           username: vidyoportalRegistry
           password: LZT5JSdfzgZfQpWoFSlxxR7bFcRszOR0jpCYe6yB2k+ACRCi3Agt
      - run: |
          docker build . -t vidyoportalregistry.azurecr.io/vidyoportal:${{ github.sha }}
          docker push vidyoportalregistry.azurecr.io/vidyoportal:${{ github.sha }}
